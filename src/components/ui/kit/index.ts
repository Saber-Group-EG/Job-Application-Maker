// The dashboard's standard UI building blocks (slate borders, white cards,
// text-sm controls). They started life on the promo pages; import them from
// here on every other page.
export * from '../../../pages/promos/components/PromoUI';
